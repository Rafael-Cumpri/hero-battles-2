const express = require('express');
const router = express.Router();
const  = require('');

router.post('/heroes', );
router.get('/heroes', );
router.get('/heroes/:param', );
router.put('/heroes/:id', );
router.delete('/heroes/:id', );

module.exports = router;